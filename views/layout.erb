<html>
  <meta charset="UTF-8">
    <title>Contact List</title>
  </meta>
  <body>

    <h1>Contact List</h1>

    <% if session[:message] %>
      <div class="message">
        <p><%= session.delete(:message) %></p>
      </div>
    <% end %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>